#ifndef _OCTREE_OCTREE_SAMPLES_
#define _OCTREE_OCTREE_SAMPLES_

#include <cstring>

namespace octree {

const unsigned char* get_one_octree(const char *name, size_t *size = nullptr);

} // namespace octree

#endif // _OCTREE_OCTREE_SAMPLES_